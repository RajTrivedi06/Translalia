# Translalia LLM Context

## Status
Deprecated reference-only document.

## Do not load this by default
- Start with `docs/05-llm/DOC_MAP.md`.
- Use the root context packs in `docs/05-llm/context-packs/`.
- Use `translalia-web/docs/PROMPTS.md` and `translalia-web/docs/TRANSLATION_PIPELINE.md` for current app-local details.

## Read this only if
- You are tracing legacy terminology still present in old comments or temporary notes.
- You need historical context for the older archetype-heavy prompt system.
- A task explicitly points you to this file.

## Known limitations
- Parts of the earlier narrative around prompts and pipeline behavior are no longer current.
- Simplified-prompt mode and current batch-job behavior are documented elsewhere.
- This file is retained to avoid breaking older references, not as a canonical source.
